<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.cmpe277project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".LibraryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".module.auth.OnBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".module.cart.CartActivity" />
        <activity android:name=".module.librarian.AddBookActivity"></activity>
        <activity android:name=".module.auth.DashBoardActivity" />
        <activity android:name=".module.librarian.SearchActivity" />
        <activity android:name=".module.librarian.UpdateBookActivity"></activity>
        <activity android:name=".module.patron.IssuedBooksActivity"></activity>
        <activity android:name=".module.patron.PatronSearchActivity"></activity>
        <activity android:name=".module.patron.BookDetailActivity"></activity>

    </application>

</manifest>